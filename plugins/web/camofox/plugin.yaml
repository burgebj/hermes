name: web-camofox
version: 1.0.0
description: "Camofox local browser-backed web search and extraction. Uses CAMOFOX_URL and no external search/extract API key."
author: NousResearch
kind: backend
provides_web_providers:
  - camofox
